source "https://rubygems.org"

# Use the GitHub Pages gem so the local and CI build environments match.
# See https://jekyllrb.com/docs/continuous-integration/github-actions/
gem "github-pages", group: :jekyll_plugins